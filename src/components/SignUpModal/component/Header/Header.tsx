import React from 'react';
import styled from 'styled-components';

const HeaderContainer = styled.div`
  font-size: 1.9rem;
  font-weight: 500;
  display: flex;
  color: white;
  justify-content: center;
`;

const Header = () => <HeaderContainer>Sign Up</HeaderContainer>;

export default Header;
